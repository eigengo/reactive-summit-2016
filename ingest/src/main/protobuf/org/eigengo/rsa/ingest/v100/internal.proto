syntax = "proto3";

package org.eigengo.rsa.ingest.v100;

message SimplifiedTweet {
    string handle = 1;
    repeated string mediaUrls = 2;
}

message TweetImage {
    string handle = 1;
    bytes content = 2;
}
